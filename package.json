{
  "name": "cloudlibrary-launch-checks",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only auth,firestore,storage \"node --test tests/firestore.rules.test.js\"",
    "check": "node --check functions/index.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase-tools": "^13.32.0"
  }
}
